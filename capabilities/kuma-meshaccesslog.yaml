naftiko: 1.0.0-alpha2
info:
  label: Kuma API — MeshAccessLog
  description: 'Kuma API — MeshAccessLog. 4 operations. Lead operation: Returns a list of MeshAccessLog in the mesh.. Self-contained
    Naftiko capability covering one Kuma business surface.'
  tags:
  - Kuma
  - MeshAccessLog
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    KUMA_API_KEY: KUMA_API_KEY
capability:
  consumes:
  - type: http
    namespace: kuma-meshaccesslog
    baseUri: ''
    description: Kuma API — MeshAccessLog business capability. Self-contained, no shared references.
    resources:
    - name: meshes-mesh-meshaccesslogs
      path: /meshes/{mesh}/meshaccesslogs
      operations:
      - name: getmeshaccessloglist
        method: GET
        description: Returns a list of MeshAccessLog in the mesh.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: offset
          in: query
          type: integer
          description: offset in the list of entities
        - name: size
          in: query
          type: integer
          description: the number of items per page
        - name: filter
          in: query
          type: object
          description: filter by labels when multiple filters are present, they are ANDed
        - name: mesh
          in: path
          type: string
          description: name of the mesh
          required: true
    - name: meshes-mesh-meshaccesslogs-name
      path: /meshes/{mesh}/meshaccesslogs/{name}
      operations:
      - name: getmeshaccesslog
        method: GET
        description: Returns MeshAccessLog entity
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: mesh
          in: path
          type: string
          description: name of the mesh
          required: true
        - name: name
          in: path
          type: string
          description: name of the MeshAccessLog
          required: true
      - name: putmeshaccesslog
        method: PUT
        description: Creates or Updates MeshAccessLog entity
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: mesh
          in: path
          type: string
          description: name of the mesh
          required: true
        - name: name
          in: path
          type: string
          description: name of the MeshAccessLog
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deletemeshaccesslog
        method: DELETE
        description: Deletes MeshAccessLog entity
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: mesh
          in: path
          type: string
          description: name of the mesh
          required: true
        - name: name
          in: path
          type: string
          description: name of the MeshAccessLog
          required: true
    authentication:
      type: bearer
      token: '{{env.KUMA_API_KEY}}'
  exposes:
  - type: rest
    namespace: kuma-meshaccesslog-rest
    port: 8080
    description: REST adapter for Kuma API — MeshAccessLog. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/meshes/{mesh}/meshaccesslogs
      name: meshes-mesh-meshaccesslogs
      description: REST surface for meshes-mesh-meshaccesslogs.
      operations:
      - method: GET
        name: getmeshaccessloglist
        description: Returns a list of MeshAccessLog in the mesh.
        call: kuma-meshaccesslog.getmeshaccessloglist
        with:
          offset: rest.offset
          size: rest.size
          filter: rest.filter
          mesh: rest.mesh
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/meshes/{mesh}/meshaccesslogs/{name}
      name: meshes-mesh-meshaccesslogs-name
      description: REST surface for meshes-mesh-meshaccesslogs-name.
      operations:
      - method: GET
        name: getmeshaccesslog
        description: Returns MeshAccessLog entity
        call: kuma-meshaccesslog.getmeshaccesslog
        with:
          mesh: rest.mesh
          name: rest.name
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: putmeshaccesslog
        description: Creates or Updates MeshAccessLog entity
        call: kuma-meshaccesslog.putmeshaccesslog
        with:
          mesh: rest.mesh
          name: rest.name
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deletemeshaccesslog
        description: Deletes MeshAccessLog entity
        call: kuma-meshaccesslog.deletemeshaccesslog
        with:
          mesh: rest.mesh
          name: rest.name
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: kuma-meshaccesslog-mcp
    port: 9090
    transport: http
    description: MCP adapter for Kuma API — MeshAccessLog. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: returns-list-meshaccesslog-mesh
      description: Returns a list of MeshAccessLog in the mesh.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: kuma-meshaccesslog.getmeshaccessloglist
      with:
        offset: tools.offset
        size: tools.size
        filter: tools.filter
        mesh: tools.mesh
      outputParameters:
      - type: object
        mapping: $.
    - name: returns-meshaccesslog-entity
      description: Returns MeshAccessLog entity
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: kuma-meshaccesslog.getmeshaccesslog
      with:
        mesh: tools.mesh
        name: tools.name
      outputParameters:
      - type: object
        mapping: $.
    - name: creates-updates-meshaccesslog-entity
      description: Creates or Updates MeshAccessLog entity
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: kuma-meshaccesslog.putmeshaccesslog
      with:
        mesh: tools.mesh
        name: tools.name
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: deletes-meshaccesslog-entity
      description: Deletes MeshAccessLog entity
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: kuma-meshaccesslog.deletemeshaccesslog
      with:
        mesh: tools.mesh
        name: tools.name
      outputParameters:
      - type: object
        mapping: $.
